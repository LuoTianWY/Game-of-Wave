# 游玩界面需求表（拼图主界面）

> **参考分辨率**：1920×1080（`CanvasScaler` → `ScaleWithScreenSize`）。
> **素材约定**：尺寸均为参考像素，Unity 导入时选 `Sprite (2D and UI)`，PNG 格式，透明底。
> **核心原则**：所有**波形本身都是程序化绘制**（无美术资源）；美术资源只承担背景 / 图标 / 立绘 / 按钮。
> 本文档为**持续追加**的需求表：美术素材需求、程序需求、缺少的设计需求三块会随讨论继续扩展。

---

## 一、已确认决策（本轮锁定）

| # | 项 | 决策 |
|---|---|---|
| 1 | 板子坐标系 | 横向 = 时间（拍），纵向 = 固定声部轨（Q1 选 a） |
| 2 | 架构 | 角色库持久 + 每任务独立板 + **总分队编排板**（长期默认任务：专辑/训练） |
| 3 | 角色占用 | 同一角色不可被多任务占用 → 直接锁定（原型期单任务，不测多任务） |
| 4 | 头段 | 锁定，不可移动 / 删除 |
| 5 | 重叠 | 完全禁止同轨时间重叠；不同轨自由重叠（= 和声） |
| 6 | 角色唯一性 | 唯一实例；部分角色带 `reusable` 特性可多放（如鼓组） |
| 7 | 快捷键 | Ctrl+S = 确定；Ctrl+Z = 撤销一步；Esc = 返回上级（预留接口） |
| 8 | 确定/取消 | 确定 = 保存并留屏；取消 = Esc = 丢弃并返回 |
| 9 | 音效预览 | 占位音（按角色音高播短音），预留波形→音频合成接口 |
| 10 | 缩放 | clamp [0.5×, 3×]；进任务自动定位最左端 |
| 11 | 脱视特效 | 左缘「◀ 回到起点」箭头，点击瞬移回最左 |
| 12 | 侧栏 | 右缘窄标签，点击展开 / 收起 |
| 13 | 保存 | 内存快照 + JSON 序列化接口占位 |
| 14 | 命中优先级 | 命中波形则移动波形，否则平移视口 |

---

## 二、美术素材需求

> 导入接口统一走 `Resources.Load<Sprite>("<路径>")`，缺省时**回退为程序化色块**，保证无素材也能跑通原型。

| 编号 | 素材名 | 位置 / 用途 | 建议尺寸(px) | 导入接口（Resources 路径） | 说明 |
|---|---|---|---|---|---|
| A1 | 主窗口边框 | 拼图窗口装饰边框 | 边框宽 16px，9-slice | `UI/frame` | 宽度可调 |
| A2 | 底版背景 | 拼图区背景 | 512×512 无缝平铺 | `UI/baseplate` | 平铺铺满整板 |
| A3 | 抬头栏背景 | 顶部任务信息条 | 1920×72，9-slice | `UI/header` | 可程序化纯色替代 |
| A4 | 右侧栏面板 | 角色库展开面板 | 320×1080，9-slice | `UI/sidebar` | |
| A5 | 侧栏标签 | 展开/收起把手 | 24×120 | `UI/sidebar_tab` | 收起态贴右缘 |
| A6 | 角色卡片底 | 角色库卡片 | 280×140，9-slice | `UI/card_bg` | 含波形预览区 |
| A7 | 角色头像 | 每角色一张 | 128×128（@2x 256） | `Characters/<名字>` | 已有接口，占位可用色块 |
| A8 | 播放图标 | 卡片试听按钮 | 40×40 | `UI/icon_play` | |
| A9 | 左侧栏背景 | 既有波形（头段）栏 | 宽 64、全高 1080，9-slice | `UI/headbar` | |
| A10 | 回到起点箭头 | 脱视提示 | 64×64 | `UI/icon_back` | 吸左缘 |
| A11 | 确定按钮 | 提交区 | 200×60，9-slice | `UI/btn_confirm` | 复用通用按钮 |
| A12 | 取消按钮 | 提交区 | 200×60，9-slice | `UI/btn_cancel` | 同上 |
| A13 | 合法落点高亮 | 拖拽反馈 | 128×64，9-slice | `UI/highlight_ok` | 可程序化替代 |
| A14 | 非法落点高亮 | 拖拽反馈 | 128×64，9-slice | `UI/highlight_bad` | 可程序化替代 |

**程序化绘制、无需美术**：节拍竖线、声部轨横线、所有波形曲线、撤销/占用置灰遮罩。

---

## 三、程序需求

> 命名空间沿用 `WaveTeam.Core`（纯数据）/ `WaveTeam.Audio` / `WaveTeam.UI`。现有可复用：`Core/Tile`、`Core/Character`、`Core/ChainTimeline`、`UI/DraggableCard`、`UI/UIFactory`。

### P1 · 编排板组件（可复用：任务板 / 总分队板）
- 数据模型：`BoardArrangement = { 头段引用, List<PlacedCharacter>{ characterId, track, startBeat } }`。
- 坐标变换：`beatToScreen`（offset + scale），支撑平移/缩放。
- 波形程序化绘制（LineRenderer / UI mesh）。
- 放置校验：同轨时间重叠禁止、头段锁定、唯一实例（`reusable` 角色例外）。
- 撤销栈（place / move / remove，建议深度 32）。
- 复用点：任务板与总分队板共用此组件，仅数据源不同。

### P2 · 视口控制器
- 拖拽空白平移；WASD 平移；滚轮缩放（clamp [0.5×, 3×]，锚点建议鼠标）。
- 进入任务自动定位最左端。
- 左缘「回到起点」箭头：视口脱离头段时显示，点击瞬移回最左。

### P3 · 右侧角色库
- 右缘标签点击展开/收起。
- 卡片：波形预览 + 音效预览（占位音）+ 拖出。
- 拖出→放置；右键已放置波形→收回；拖回侧栏→收回。
- 唯一实例：占用后卡片置灰；`reusable` 角色可多放。

### P4 · 左侧既有波形栏
- 渲染任务头段并锁定。
- 脱视特效：箭头 + 点击回最左（见 P2）。

### P5 · 抬头栏
- 任务名 + 信息（目标性质 / 风格 / 期限）。

### P6 · 提交与快捷键
- 确定 = 保存快照并留屏；取消 / Esc = 丢弃并返回；Ctrl+Z = 撤销一步；Ctrl+S = 确定。
- 保存 = 内存快照 + JSON 序列化接口占位。

### P7 · 音效预览接口
- `IPreviewPlayer`：占位音实现（按角色音高播短音），接口留待未来波形→音频合成。

### P8 · 素材加载器
- 统一 `UIResource`：`Resources.Load<Sprite>("UI/<id>")`，缺省回退程序化色块（对应二表 A1–A14）。

### P9 · 角色数据扩展
- `Character` 增 `reusable` 标志与占用状态 `isBusy`（锁机制）。
- `Task` 增：头段（一条 chain）、目标性质、风格、期限。

---

## 四、缺少的设计需求（待定，标红）

> 以下各项为**尚未明确**、需后续补齐的设计点。标红项不影响先搭板子骨架，但会卡住对应模块的最终数值/形态。

1. <span style="color:#d73a49">【未明确】声部轨数：固定几轨？</span> —— 建议暂定 4 轨，作为占位默认值。
2. <span style="color:#d73a49">【未明确】是否允许跨轨：单个角色能否占多轨（和弦型角色）？</span> —— 原型期建议**不支持跨轨**（每角色单轨）。
3. <span style="color:#d73a49">【未明确】头段具体规格：长度 / 波形来源 / 是否每任务不同？</span>
4. <span style="color:#d73a49">【未明确】抬头栏「任务信息」的具体字段与展示形式：目标性质（稳定度/张力/温度/冲劲）用数值、仪表还是图标表达？</span>
5. <span style="color:#d73a49">【未明确】总分队编排板的长期任务机制：专辑 / 训练如何运转？与单任务锁的时序关系？</span>
6. <span style="color:#d73a49">【未明确】占位音规则：按角色基础音高？音色固定还是取特质？</span>（次要）
7. <span style="color:#d73a49">【未明确】缩放锚点：以鼠标为中心还是屏幕中心？</span>（次要，可默认鼠标）

> 另：第 5 项「总分队长期任务」与 README 第 8 节日常循环 / 第 10 节演出系统强相关，建议后续合并讨论，避免两处口径不一。
