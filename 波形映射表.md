# 波形 · 音色 · 性格 映射表（草案）

> **这是草案**：请直接改「文件名」列，匹配你实际导入的 ogg；其余列（波形类型、端点段、性格名词）跟着文件名走。
> **映射链**：ogg 文件名 → 波形类型 →（头/尾端点段 + 波形形状）→ 性格名词。
> **端点模型**：波形占一个固定大小单元，从上到下分 **4 段**（1=顶 … 4=底）；相邻波形 **尾端点段 == 头端点段 → 接上（契合）**。

| ogg 文件名 | 波形类型 | 波形形状（显示） | 头端点段 | 尾端点段 | 性格名词 |
|---|---|---|---|---|---|
| kick | 冲击快衰减（底鼓） | 低频、急起急落、短 | 1 | 3 | 沉稳 |
| snare | 噪声爆发（军鼓） | 中频、极短促爆裂 | 3 | 1 | 干脆 |
| hihat | 短脉冲（踩镲） | 高频、极短 | 1 | 4 | 跳脱 |
| crash | 长衰减（吊镲） | 高频、渐弱长尾 | 4 | 2 | 张扬 |
| tom | 圆润中衰减（通鼓） | 中低频、圆润 | 2 | 3 | 温厚 |
| ride | 持续通透（叮叮镲） | 高频、持续余音 | 3 | 4 | 通透 |
| clap | 锐利爆发（拍手） | 中高频、干脆 | 4 | 1 | 爽朗 |
| shaker | 细碎连抖（沙锤） | 高频、细密短颤 | 2 | 2 | 细碎 |
| rimshot | 犀利边击（鼓边） | 中高频、极利落 | 1 | 2 | 犀利 |

---

**说明**

- 端点段分布刻意留了一条**可发现的完整链**（每个箭头满足「尾段==下一头段」）：

  `kick(1→3) → snare(3→1) → hihat(1→4) → crash(4→2) → tom(2→3) → ride(3→4) → clap(4→1) → rimshot(1→2) → shaker(2→2)`

  其中 shaker 尾段=头段（2→2），可自环，也可接回 tom。
- 每个波形只能接上 **2~3 种**其它波形（snare / clap 接 3 种，其余接 2 种），其余 6~7 种组合都是「异段不契合」——呼应"刻意做不契合波形"的想法，让共振反馈明显可见。
- 想**孤立**某几个波形：把它头/尾端点段改成没人用的段即可。若以后要更多段，直接改段数（程序侧本就按整数段通用，不受 4 限制）。
- 「波形形状」只决定**显示**的曲线；具体形状参数（频率段、起落、时长）在程序任务 C1 / B1 落实。
- 多个变体（如 `kick_soft` / `kick_hard`）可拆多行、各配名词；也可共用同一波形、只换音色。
