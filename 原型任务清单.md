# 原型任务清单（拼图主界面 · 鼓组任务）

> **目标**：2 天内跑通「补齐鼓组」的拼图主界面。本文档是**执行清单**，按工种分列，可逐项勾掉。
> **配套**：与 [游玩界面需求表.md](游玩界面需求表.md) 对应（需求 =「做什么」，本清单 =「谁做、按什么顺序做」）；波形/音色/名词对应关系见 [波形映射表.md](波形映射表.md)。
> **已定简化**：原型先做**单轨（鼓轨）**；**无胜利条件**，用「演出！」按钮（= 播放）代替。
> **端点模型**：每个波形占一个**固定大小单元**，单元从上到下平分为 **4 段**（1=顶 … 4=底）；波形有**头端点**（左）与**尾端点**（右），各落在其中一段。相邻波形 **尾端点段 == 头端点段 → 接上（契合）**，异段不契合（供 C10 判定）。

## 最终效果

1. 启动进入拼图主界面，背景循环播放一段「缺了鼓组」的音乐（BGM），明显少了打击乐。
2. 右侧角色栏展开，若干角色卡，每张显示：一段波形预览 + 一个性格名词，点一下可试听该角色的鼓音色。
3. 每次启动，角色随机分到不同波形 / 名词 / 音色。
4. 玩家把角色卡拖到鼓轨时间轴上摆成一排鼓点；可拖动、右键收回、撤销。
5. 相邻波形若「接得上」，代表互相契合。在此阶段，我们会刻意做一两个不那么契合的波形。
6. 点「演出！」：BGM 与已摆好的鼓声叠加播放，听到补全后的曲子。
7. 可平移 / 缩放查看整条鼓轨。

---

## A · 音频素材（导入，你来做）

| # | 任务 | 验收 | 对应需求表 |
|---|---|---|---|
| A1 | 导入缺失鼓组的 BGM → `Resources/Audio/bgm` | BgmPlayer 能循环播放 | P7 |
| A2 | 导入鼓音色 ogg → `Resources/Audio/drums/<类型>.ogg` | 文件名即波形类型 | — |

## B · 策划数据（我起草、你定稿）

| # | 任务 | 验收 |
|---|---|---|
| B1 | 波形类型 + 形状参数 + **头/尾端点段（1–4）** | 每类型画出曲线，端点落在指定段 |
| B2 | ogg 文件名 → 波形 映射（见 [波形映射表.md](波形映射表.md)） | 传文件名能拿到波形 |
| B3 | 波形 → 性格名词 映射 | 波形能显示对应名词 |

## C · 程序（核心）

### C-波形与数据
| # | 任务 | 验收 |
|---|---|---|
| C1 | 波形类型枚举 + 形状参数（含**头/尾端点段**，对应 B1） | 可被渲染器与契合判定消费 |
| C2 | 波形渲染器 `WaveformRenderer`（程序化曲线，画在固定大小单元内、可视 4 段分区与端点；卡片/板子/预览三处复用） | 一处调用三处渲染 |
| C3 | ogg 文件名 → 波形 解析加载器（扫 `Audio/drums` 目录匹配） | 读目录自动建映射 |

### C-角色库
| # | 任务 | 验收 |
|---|---|---|
| C4 | 启动随机分配：角色 ← 随机波形 + 绑定 AudioClip + 名词 | 每次启动波形不同 |
| C5 | 角色卡 UI：波形预览 + 名词 + 试听按钮（试听 = 播该 ogg） | 点卡能听该角色音色 |
| C6 | 侧栏展开/收起 + 卡片拖出（复用 `DraggableCard`） | 卡片能拖进板子 |

### C-编排板（鼓轨单轨）
| # | 任务 | 验收 |
|---|---|---|
| C7 | 时间轴坐标系 `beatToScreen`（复用 `ChainTimeline`） | 波形按拍落位 |
| C8 | 放置 / 移动 / 右键收回 + 同轨不重叠 + 鼓角色可复用 | 能摆、能挪、能收、不能叠 |
| C9 | 视口平移 / 缩放 / 回起点 | WASD / 滚轮 / 左缘箭头可用 |

### C-契合与演出
| # | 任务 | 验收 |
|---|---|---|
| C10 | 端点契合度计算：相邻波形**头/尾端点同段即相接**（异段不契合） | 同段=接上，异段=接不上 |
| C11 | 共振颤动（程序化顶点位移，幅度 = 契合度，实时） | 接得上就颤、幅度随曲风契合度变 |
| C12 | 「演出！」按钮 = 播放（BGM + 已放鼓声叠加） | 听到补全后的整曲 |

### C-提交与快捷键
| # | 任务 | 验收 |
|---|---|---|
| C13 | 确定 / 取消 + 内存快照 + JSON 占位 | 确定存、取消丢 |
| C14 | Ctrl+S / Ctrl+Z / Esc | 三键可用 |

### C-素材接口
| # | 任务 | 验收 |
|---|---|---|
| C15 | `UIResource` 加载器 + 缺省回退色块（对应需求表 A1–A14） | 无素材也能跑通 |

## D · 美术（原型可全占位）

| # | 任务 | 验收 |
|---|---|---|
| D1 | UI 素材（边框/底版/卡片底/按钮/图标，见需求表 A1–A14） | 先全用占位色块，波形程序化 |

---

**建议推进顺序**：A（素材就位）→ B（定映射）→ C1–C3（波形与数据）→ C4–C6（角色库）→ C7–C9（编排板）→ C10–C12（契合与演出）→ C13–C15（收尾）。D 全程占位、最后再补。
